mode:
  memory: remote
  vision: remote
  ontology: remote
  embedding: remote
  recommendation: remote
  planner: remote

services:
  vision: "http://localhost:5002"
  ontology: "http://localhost:5003"
  embedding: "http://localhost:5004"
  memory: "http://localhost:5001"
  recommendation: "http://localhost:5005"
  planner: "http://localhost:5006"

timeout:
  default: 3000