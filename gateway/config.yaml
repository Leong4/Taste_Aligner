mode:
  memory: remote
  vision: remote
  ontology: remote
  embedding: remote
  recommendation: remote
  planner: remote

services:
  memory: "http://localhost:5001/read"
  vision: "http://localhost:5002/describe"
  ontology: "http://localhost:5003/normalize"
  embedding: "http://localhost:5004/generate"
  recommendation: "http://localhost:5005/score"
  planner: "http://localhost:5006/compose"

timeout:
  default: 3000