# Embedding Service Dependencies

# Core web framework
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.0.0

# Numerical computing (required by both backends)
numpy>=1.24.0

# sentence-transformers backend (st_v1)
# Pulls in torch, transformers, tokenizers automatically.
# Pin to a stable minor version to avoid surprise model-API changes.
sentence-transformers>=2.7.0,<4.0.0
