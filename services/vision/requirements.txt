# Vision Service Dependencies (v2)
# CLIP-only vision backend (clip_v1).

# Core — always required
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.0.0
Pillow>=10.0.0

# clip_v1 backend — required (default and only supported backend)
# Pulls in PyTorch (~2 GB on first install).
# Install: pip install open_clip_torch
open_clip_torch>=2.20.0
